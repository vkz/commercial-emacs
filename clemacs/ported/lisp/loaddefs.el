;;; loaddefs.el --- clemacs stub for generated autoloads  -*- lexical-binding: t; -*-

;; This file is a clemacs bring-up stub.
;;
;; Upstream `lisp/loaddefs.el` is generated (and very large).  For clemacs
;; bring-up we rely on the checked-in snapshot `lisp/ldefs-boot.el` to avoid
;; making `make -C lisp autoloads` a hard prerequisite for loading startup
;; manifests.

(provide 'loaddefs)

;;; loaddefs.el ends here
